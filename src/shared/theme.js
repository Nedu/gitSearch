import { createMuiTheme } from '@material-ui/core/styles';

export const COLORS = {
    white: '#ffffff',
    black: '#000000',
};


const theme = createMuiTheme({
    // palette: {
    //     primary: { },
    //     secondary: { },
    // },
});

export default theme;
